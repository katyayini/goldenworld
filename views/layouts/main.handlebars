<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>The Tech Blog</title>
  <link rel="stylesheet" href="/css/style.css">
</head>

<body>
  <div class="flex-column justify-space-around">
    <header style="background-color: #B82C0E;" class="display-flex-column align-center p-2">
       <nav class="p-2">
        <!-- Conditionally render login or logout links -->
         <a style="text-decoration: none; color: #d1c9cb;" class="link mr-5" href="/">Home</a>
        <a style="text-decoration: none; color: #d1c9cb;" class="link mr-5" href="/dashboard">Dashboard</a>
        {{#if logged_in}}
         <button style="background-color: #B82C0E; color: #d1c9cb; border-style: none" class="no-button" id="logout">Logout</button>
        {{else}}
        <a style="text-decoration: none; color: #d1c9cb;" class="mr-5" href="/login">Login</a>
        {{/if}}
      </nav>
      
      <h1>
       <a style="color: #d1c9cb" href="/">The Tech Blog</a>
      </h1>
    </header>
    <main class="container container-fluid mt-5">
      <!-- Render the sub layout -->
      {{{ body }}}
    </main>
    <footer>
      
    </footer>
  </div>

  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>
